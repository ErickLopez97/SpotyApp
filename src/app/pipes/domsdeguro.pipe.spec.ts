import { DomsdeguroPipe } from './domsdeguro.pipe';

describe('DomsdeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new DomsdeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
